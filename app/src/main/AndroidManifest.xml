<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.acadmeychatting">


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity android:name=".MainActivity">
        </activity>

        <activity android:name=".LoginNsigNup.LoginActivity"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginNsigNup.RegisterActivity"
            tools:ignore="InnerclassSeparator" />

        <activity
            android:name=".OptionMenu.SettingsActivity"
            android:parentActivityName=".MainActivity"
            tools:ignore="InnerclassSeparator" />

        <activity android:name=".Chat.GroupChatActivity"
            tools:ignore="InnerclassSeparator" />

        <activity android:name=".LoginNsigNup.PhoneLoginActivity"
            tools:ignore="InnerclassSeparator" />

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <activity
            android:name=".OptionMenu.FindFriendsActivity"
            android:parentActivityName=".MainActivity"
            tools:ignore="InnerclassSeparator" />

        <activity android:name=".OptionMenu.ProfileActivity"
            tools:ignore="InnerclassSeparator" />

        <activity
            android:name=".Chat.ChatActivity"
            android:parentActivityName=".MainActivity"
            tools:ignore="InnerclassSeparator" />
    </application>

</manifest>